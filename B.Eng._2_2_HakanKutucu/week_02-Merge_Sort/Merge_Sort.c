#include <stdio.h>
#include <stdlib.h>


void mergeSort(int Array[]) {
    if (len(Array) == 1)
        return;
    mergeSort();
    mergeSort();

    Array = 
}

int len(int Array[]) {
    return sizeof(Array) / sizeof(int);
}
